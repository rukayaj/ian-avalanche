services:
  extractor:
    build: .
    env_file:
      - .env.local
    environment:
      MODEL_NAME: ${MODEL_NAME:-gpt-5}
      EXTRACT_REPEATS: ${EXTRACT_REPEATS:-3}
    volumes:
      - .:/app
    working_dir: /app
    entrypoint: ["python", "-m", "src.run_batch_pipeline"]
